div(ng-hide='answers')
    alert(type='warning') Aún no hay respuestas para esta solicitud

ul(ng-show='answers')
    li(ng-repeat='answer in answers')
        |{{ answer.evaluation }}

        br

        small
            |Por {{ answer.creator.name }}
