.flash(ng-repeat='flash in list')
  .alert(
    ng-class='"alert-" + flash.type',
    ng-click='dismiss($index)')

    i.glyphicon(ng-class='"glyphicon-" + (icons[flash.type] || "info-circle")')
    strong {{ flash.title }}
    span {{ flash.message }}
