div.col-md-4.col-md-offset-4
    div.panel.panel-default
        div.panel-heading
            h3.panel-title
                i.fa.fa-sr.fa-user
                |Recuperar cuenta

        form(
            name='form'
            role='form'
            ng-submit='form.$valid && submit()'
            novalidate)

            div.panel-body
                div.form-group
                    div.input-group
                        span.input-group-addon
                            i.fa.fa-at.fa-fw

                        input.form-control(
                            ng-model='form.email',
                            type='email',
                            placeholder='Ingrese su email',
                            required)

            div.panel-footer.text-right
                a.btn.btn-link.pull-left(ng-href='/') Ya tengo mi clave

                button.btn.btn-default(
                    type='submit'
                    ng-disabled='form.$invalid')

                    i.fa.fa-sr.fa-sign-in
                    |Enviar

    div(ng-show='success')
        alert(type='success') ¡Tu clave ha sido restaurada! Por favor revisa tu cuenta de correo. No olvides revisar la carpeta SPAM.

    div(ng-show='error')
        alert(type='danger') ¡Email inválido!
