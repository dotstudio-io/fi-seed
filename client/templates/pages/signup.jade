div.col-md-4.col-md-offset-4
    div.panel.panel-default
        div.panel-heading
            h3.panel-title
                i.fa.fa-sr.fa-pencil
                |Crear Cuenta

        form(
            ng-submit='form.$valid && submit()',
            name='form',
            role='form',
            novalidate)

            div.panel-body
                div.form-group
                    div.input-group
                        span.input-group-addon
                            i.fa.fa-user.fa-fw

                        input.form-control(
                            placeholder="Ingresa tu nombre",
                            ng-model='form.name',
                            type='text',
                            required)

                div.form-group
                    div.input-group
                        span.input-group-addon
                            i.fa.fa-at.fa-fw

                        input.form-control(
                            placeholder="Ingresa tu email",
                            ng-model='form.email',
                            type='email',
                            required)

                div.form-group
                    div.input-group
                        span.input-group-addon
                            i.fa.fa-fw.fa-asterisk

                        input.form-control(
                            placeholder="Ingrese su clave",
                            ng-model='form.password',
                            ng-minlength='4',
                            type='password',
                            required)

                label.input-group
                    input.hidden(type='checkbox', ng-model='form.specialist')

                    span.input-group-addon
                        i(class='fa fa-fw fa-{{ form.specialist && "check-square" || "square-o" }}')

                    span.form-control Soy médico especialista

                div.form-group(ng-show='form.specialist')
                    div.input-group
                        span.input-group-addon
                            i.fa.fa-fw.fa-user-md

                        select.form-control(
                            ng-options='specialty._id as specialty.name for specialty in specialties',
                            ng-init='form.specialty = specialties[0]._id',
                            ng-disabled='!form.specialist',
                            ng-model='form.specialty',
                            required)

            div.panel-footer.text-right
                a.btn.btn-link.pull-left(ng-href='/') Ya tengo una cuenta

                button.btn.btn-default(type='submit', ng-disabled='form.$invalid')

                    i.fa.fa-sr.fa-check
                    |Crear cuenta
